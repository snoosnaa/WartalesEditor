<Window x:Class="WartalesEditor.Views.QuickBmsExportProgressDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Export Back to Wartales"
        Width="430"
        Height="190"
        MinWidth="400"
        MinHeight="180"
        ResizeMode="NoResize"
        SizeToContent="Manual"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Export Back to Wartales"
                   Style="{StaticResource UtilityMajorHeadingTextStyle}"/>
        <TextBlock Grid.Row="1"
                   Margin="0,10,0,14"
                   Text="{Binding StageText}"
                   TextWrapping="Wrap"
                   Style="{StaticResource SupportingTextStyle}"/>
        <ProgressBar Grid.Row="2"
                     Height="16"
                     IsIndeterminate="True"
                     VerticalAlignment="Top"/>
        <Button Grid.Row="3"
                Content="Cancel"
                HorizontalAlignment="Right"
                MinWidth="88"
                IsEnabled="{Binding CanCancel}"
                Click="CancelButton_Click"/>
    </Grid>
</Window>

<Window x:Class="WartalesEditor.Views.OverworldMovementSpeedDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Title}" Width="600" Height="410"
        MinWidth="520" MinHeight="360" SizeToContent="Manual"
        WindowStartupLocation="CenterOwner" ResizeMode="CanResizeWithGrip"
        ShowInTaskbar="False">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Increase how quickly your party travels across the world map. Other roaming parties keep their normal speeds."
                   TextWrapping="Wrap" Margin="0,0,0,16"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Current setting:" FontWeight="SemiBold"
                       VerticalAlignment="Center" Margin="0,0,8,0"/>
            <TextBlock Grid.Column="1" Text="{Binding CurrentStateText}"
                       VerticalAlignment="Center"/>
        </Grid>

        <GroupBox Grid.Row="2" Header="Movement preset" Margin="0,16,0,0">
            <StackPanel Margin="12">
                <ComboBox ItemsSource="{Binding Presets}"
                          SelectedItem="{Binding SelectedPreset}"
                          DisplayMemberPath="Name" MinWidth="220"
                          HorizontalAlignment="Left"/>
                <Border Background="#FFF7E6" BorderBrush="#E0B35B"
                        BorderThickness="1" Padding="12" Margin="0,14,0,0">
                    <TextBlock Text="{Binding PreviewText}" TextWrapping="Wrap"/>
                </Border>
            </StackPanel>
        </GroupBox>

        <Button Grid.Row="3" Content="Reset to Vanilla"
                HorizontalAlignment="Left" Margin="0,14,0,0"
                Click="ResetButton_Click"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,14,0,0">
            <Button Content="Apply" Width="100" Margin="0,0,8,0"
                    IsEnabled="{Binding CanApply}" Click="ApplyButton_Click"/>
            <Button Content="Close" Width="90" Click="CloseButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
